{
  "buildCommand": "cd archon-ui-main && npm install && npm run build",
  "outputDirectory": "archon-ui-main/dist",
  "installCommand": "cd archon-ui-main && npm install --include=dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}